

run pip install requirments.txt
run zap on a localhost
get  api key from Tools>Options>API then generate a key and copy into app.py aswell as the url zap is running on (will usually be localhost:8080)

